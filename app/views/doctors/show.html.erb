<h1>Doctor: <%= @doctor.name %></h1>
Department: <%= @doctor.department %>
<h2>Appointments:</h2>
<ul>
<% @doctor.appointments.each do |a| %>
<li><%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with <%= link_to a.patient.name, patient_path(a.patient) %>
<% end %>
</ul>
